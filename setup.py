from setuptools import setup, find_packages

setup(
    name='TwoFish',
    version='1.0.0',
    description='Pure Python TwoFish implementation',
    author='K-Czaplicki',
    packages=find_packages(),
    py_modules=['TwoFish'], 
)
